receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  otlp:
    endpoint: https://gov-otlp.nr-data.net:443
    headers:
      app-api: $NR_LICENSE_KEY

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]
