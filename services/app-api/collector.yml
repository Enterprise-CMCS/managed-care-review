receivers:
  otlp:
    protocols:
      http: 'localhost:4318'

exporters:
  otlp:
    endpoint: https://gov-otlp.nr-data.net:4317
    headers:
      api-key: $NR_LICENSE_KEY

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]
