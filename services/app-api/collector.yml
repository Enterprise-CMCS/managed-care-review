receivers:
  otlp:
    protocols:
      http:

exporters:
  otlphttp/datadog:
    endpoint: https://otlp-intake.ddog-gov.com:4318
    headers:
      dd-api-key: $DD_API_KEY

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp/datadog]
