mutation submitDraftSubmission($input: SubmitDraftSubmissionInput!) {
    submitDraftSubmission(input: $input) {
        submission {
            id
            stateCode
            status
            intiallySubmittedAt
            revisions {
                revision {
                    id
                    unlockInfo {
                        updatedAt
                        updatedBy
                        updatedReason
                    }
                    submitInfo {
                        updatedAt
                        updatedBy
                        updatedReason
                    }
                    createdAt
                    submissionData
                }
            }
        }
    }
}
