mutation createQuestion($input: CreateQuestionInput!) {
    createQuestion(input: $input) {
        question {
            id
            pkgID
            dateAdded
            addedBy {
                id
                role
                email
                givenName
                familyName
                stateAssignments {
                    code
                    name
                }
            }
            documents {
                name
                s3URL
            }
        }
    }
}
