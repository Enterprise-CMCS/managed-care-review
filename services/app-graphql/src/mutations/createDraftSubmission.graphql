mutation 
    createDraftSubmission($input: CreateDraftSubmissionInput!) {
      createDraftSubmission(input: $input) {
        draftSubmission {
            program {
                name
            }
            submissionType
            submissionDescription
        }
    }
}
