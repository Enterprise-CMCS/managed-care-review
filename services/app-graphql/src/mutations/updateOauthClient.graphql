mutation UpdateOauthClient($input: UpdateOauthClientInput!) {
    updateOauthClient(input: $input) {
        oauthClient {
            id
            clientId
            clientSecret
            description
            user {
                ...oauthUserFragment
            }
            grants
            scopes
            createdAt
            updatedAt
        }
    }
} 
