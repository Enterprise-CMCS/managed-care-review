mutation createOauthClient($input: CreateOauthClientInput!) {
    createOauthClient(input: $input) {
        oauthClient {
            id
            clientId
            clientSecret
            grants
            description
            contactEmail
            createdAt
            updatedAt
        }
    }
} 