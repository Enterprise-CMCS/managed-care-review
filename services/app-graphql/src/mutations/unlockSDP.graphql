mutation unlockSDP($input: UnlockSDPInput!) {
    unlockSDP(input: $input) {
        sdp {
            id
            status
            draftRevision {
                id
                updatedAt
            }
            latestSubmittedRevision {
                id
            }
        }
    }
}
