mutation unlockStateSubmission($input: UnlockStateSubmissionInput!) {
    unlockStateSubmission(input: $input) {
        submission {
            id
            stateCode
            state {
                code
                name
                programs {
                    id
                    name
                }
            }
            status
            intiallySubmittedAt
            revisions {
                revision {
                    id
                    unlockInfo {
                        updatedAt
                        updatedBy
                        updatedReason
                    }
                    submitInfo {
                        updatedAt
                        updatedBy
                        updatedReason
                    }
                    createdAt
                    submissionData
                }
            }
        }
    }
}
