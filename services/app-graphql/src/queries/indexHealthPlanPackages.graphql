query indexHealthPlanPackages {
    indexHealthPlanPackages {
        totalCount
        edges {
            node {
                id
                stateCode
                status
                intiallySubmittedAt
                revisions {
                    node {
                        id
                        createdAt
                        unlockInfo {
                            updatedAt
                        }
                        submitInfo {
                            updatedAt
                        }
                        formDataProto
                    }
                }
            }
        }
    }
}
