query indexHealthPlanPackages {
    indexHealthPlanPackages {
        totalCount
        edges {
            node {
                id
                stateCode
                status
                initiallySubmittedAt
                revisions {
                    node {
                        id
                        createdAt
                        unlockInfo {
                            updatedAt
                        }
                        submitInfo {
                            updatedAt
                        }
                        formDataProto
                    }
                }
            }
        }
    }
}
