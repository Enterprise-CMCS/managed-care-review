query getCurrentUser {
    getCurrentUser {
        email
        state {
            code
            name
            programs {
                name
            }
        }
        role
        name
    }
}
