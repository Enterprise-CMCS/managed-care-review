query getCurrentUser {
    getCurrentUser {
        email
        state {
            code
            name
            programs {
                id
                name
            }
        }
        role
        name
    }
}
