query fetchAllStatePrograms {
    fetchAllStatePrograms {
        totalCount
        edges {
            stateCode
            stateName
            node {
                id
                name
                fullName
                isRateProgram
                isDeprecated
                deprecatedByProgramId
            }
        }
    }
}
