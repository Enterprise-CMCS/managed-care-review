query showDraftSubmission($input: ShowDraftSubmissionInput!) {
    showDraftSubmission(input: $input) {
        draftSubmission {
            id
            name
            createdAt
            program {
                name
                id
            }
            stateCode
            submissionType
            submissionDescription
        }
    }
}
