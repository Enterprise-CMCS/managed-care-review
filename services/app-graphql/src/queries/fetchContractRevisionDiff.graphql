query fetchContractRevisionDiff($input: FetchContractInput!) {
    fetchContract(input: $input) {
        contract {
            id

            revisionDiff {
                ...revisionDiffFragment
            }
        }
    }
}
