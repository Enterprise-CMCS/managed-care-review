query fetchSubmissionHistory($input: FetchSubmissionHistoryInput!) {
    fetchSubmissionHistory(input: $input) {
        contractID
        history {
            actionType
            updatedAt
            updatedReason
            updatedBy {
                email
                role
            }
        }
    }
}
