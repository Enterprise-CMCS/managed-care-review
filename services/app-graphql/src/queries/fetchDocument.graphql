query fetchDocument($input: FetchDocumentInput!) {
    fetchDocument(input: $input) {
        document {
            id
            name
            s3URL
            downloadURL
            type
            s3BucketName
            s3Key
        }
    }
}
