query fetchCurrentUser {
    fetchCurrentUser {
        email
        state {
            code
            name
            programs {
                id
                name
            }
        }
        role
        name
    }
}
