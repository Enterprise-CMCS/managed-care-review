query getAllStatePrograms {
    getAllStatePrograms {
        totalCount
        edges {
            stateCode
            stateName
            node {
                id
                name
                fullName
                isRateProgram
                isDeprecated
                deprecatedByProgramId
            }
        }
    }
}
