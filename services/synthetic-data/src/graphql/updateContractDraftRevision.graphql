mutation SyntheticUpdateContractDraftRevision(
    $input: UpdateContractDraftRevisionInput!
) {
    updateContractDraftRevision(input: $input) {
        contract {
            id
            stateCode
            status
            draftRevision {
                updatedAt
            }
        }
    }
}
