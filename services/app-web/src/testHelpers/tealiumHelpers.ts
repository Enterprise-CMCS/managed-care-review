import { TealiumClientType } from '../tealium';

export const tealiumTestClient = (): TealiumClientType => {
    return {
        initializeTealium: () => {
            return
        },
        logUserEvent: () => {
            return
        },
        logPageView: () => {
            return
        },
    }
}
