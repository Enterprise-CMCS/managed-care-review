describe('auditUploads', () => {
    it('will audit a bucket', () => {

        // empty the bucket

        // upload a mess of files -- with tags.

        // audit

        throw new Error('NO THANKS')

    })
})
