{
    "name": "@managed-care-review/common-code",
    "private": true,
    "version": "1.0.0",
    "main": "dist/index.js",
    "license": "MIT",
    "scripts": {
        "precommit": "lint-staged",
        "test": "jest --watch",
        "test:once": "jest --runInBand --ci",
        "test:coverage": "yarn test --coverage --watchAll=false",
        "lint": "tsc --noEmit && eslint . --max-warnings=0",
        "clean": "rm -rf node_modules && yarn cache clean",
        "build": "tsc --noEmit"
    },
    "lint-staged": {
        "*.{js,ts}": [
            "eslint --max-warnings=0",
            "prettier --write"
        ]
    },
    "dependencies": {
        "@managed-care-review/app-proto": "workspace:^",
        "@managed-care-review/app-web": "workspace:^",
        "dayjs": "^1.10.5",
        "url-parse": "^1.5.2",
        "uuid": "^9.0.0",
        "zod": "^3.10.1"
    },
    "devDependencies": {
        "@types/jest": "^29.5.1",
        "eslint": "^8.3.0",
        "jest": "^29.5.0",
        "jest-launchdarkly-mock": "^1.0.6",
        "lint-staged": "^13.0.1",
        "ts-jest": "^29.1.0",
        "typescript": "4.9.5"
    }
}
